fastapi==0.115.0
uvicorn[standard]==0.32.0
groq==1.0.0
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.5.2
httpx==0.27.2
python-jose[cryptography]==3.3.0
python-multipart==0.0.9

# RAG - Ingestion pipeline
qdrant-client>=1.7.0
sentence-transformers>=2.3.0
transformers>=4.36.0
# torch + torchvision are installed separately in Dockerfile (CPU-only) to reduce image size
# torchvision provides the DenseNet-121 backbone for the chest X-ray model.
pillow>=10.0.0

# RAG - Retrieval pipeline
rank-bm25>=0.2.2

# RAG - Generation
anthropic>=0.18.0

# RAG - Knowledge Graph
neo4j>=5.15.0

# ECG signal analysis
# tensorflow-cpu keeps the image small; the model is a tiny Keras 3 CNN.
tensorflow-cpu>=2.16.0
# Optional R-peak detection for raw single-lead signals (long-format uploads).
neurokit2>=0.2.10
numpy>=1.26.0


